apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opspilot.fullname" . }}-test-connection"
  labels:
    {{- include "opspilot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opspilot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
